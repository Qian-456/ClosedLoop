# 执行状态回归验收

## 一条命令，查看实际测试报告

在此 PR 分支的仓库根目录、安装了 `backend/requirements.txt` 依赖的 Python 环境执行：

```powershell
python -X utf8 scripts/acceptance_report.py
```

无需配置模型 Key，无需启动服务。该命令仅执行四个固定测试模块；这些测试使用模拟执行事件或直接检查状态合并/费用函数，不会发起商家交易或真实支付。

命令输出本次 `acceptance-runs/<UTC时间>-<随机ID>/index.html` 的绝对路径。复制路径用浏览器打开，即可看到中文摘要与逐项结果。每次创建新目录，不覆盖历史报告。目录内同时保存：

- `index.html`：可直接查看的报告，无外部脚本或资源。
- `report.json`：逐项状态、源码行号、提交、工作树状态、Python/依赖版本及源码 SHA-256。
- `unittest.txt`：原始测试输出和失败堆栈，仅保留在本地。

返回码 0 表示所有实际运行项目完整通过；失败、导入错误、跳过或零测试均返回 1。查看本次命令输出的路径，不能用旧报告替代失败结果。

## 你能手动验收什么

1. 执行上面的命令，打开它输出的 HTML。本 PR 基线预计显示 **9 / 9 项全部通过**。
2. 展开“测试标识与实现位置”，找到每项对应的测试文件与断言。
3. 点击 `report.json`，检查提交是否对应你正在验收的分支；`dirty=true` 表示运行时有未提交文件，不能把它当成纯提交验证。源码哈希用于进一步核对。
4. 打开 `unittest.txt`，检查 `Ran 9 tests` 与 `OK`，不能只看绿色摘要。
5. 验证报告不会掩盖失败：运行下面命令。其中内层用例故意失败/跳过，外层测试确认报告把它们判为“不完整”，因此外层预计通过 4 项。

```powershell
python -X utf8 -m unittest scripts.test_acceptance_report -v
```

若缺少依赖，报告应显示 `error` 和未完整通过；不要将安装失败当作业务缺陷，也不要跳过报错后声称验收完成。整个 Python 无法启动或磁盘不可写时可能无法生成报告，应直接排查命令错误。

## 九项检查覆盖什么

| 测试模块 | 项数 | 实际验证 |
|---|---:|---|
| `test_execute_tool_consistency_payment_gate` | 3 | 模拟执行结果一致时进入待支付；费用超额拦截；条目失败转入补齐 |
| `test_execute_tool_hitl_timeout` | 1 | 需要用户选择时及时返回 `needs_fixup`，不一直等事件流 |
| `test_adjust_and_execute_update` | 2 | 合并成功执行结果后回写 `executed`；仍失败时保留补齐路由 |
| `test_execute_cost_consistency` | 3 | 礼物配送费用、旧数据费用回退、普通项目费用取值 |

这些是已有业务测试，本 PR 新增的是报告入口和报告准确性检查。成功状态合并测试不代表真的完成了端到端预约；错误费用测试中的快照恢复调用也不等于生产账务事务回滚。

## HR 和技术面试官分别看什么

**HR**：可以用 README 中的启动入口或仓库既有演示材料理解产品，再用本报告说明“对执行状态和费用边界做了可重复验证”。报告本身是工程证据，不是完整产品演示。

**技术面试官**：挑选一项，如“条目失败进入补齐”，依次打开：

1. `backend/src/tests/test_execute_tool_consistency_payment_gate.py` 中固定方案和模拟失败事件；
2. `backend/src/closedloop/graph/tools/execute_tool.py` 中一致性检查、`needs_fixup` 分支和状态更新；
3. 本次报告中的通过结果与源码哈希。

项目主流程可以讲为：自然语言需求 → Agent 状态路由 → Plan Subgraph 的 retrieve/filter/rerank/planner → 用户确认 → 待支付/Mock 执行 → 执行结果一致性检查 → 失败进入补齐并由用户选择 → 回写新结果。具体待支付与库存提交先后以当前执行链路为准，不能从单元测试推断真实扣款。

关键技术取舍与本次贡献见 [DECISIONS.md](DECISIONS.md)。不能将此 PR 描述为用户独立开发了业务引擎或亲自完成全部验收。

## 真实浏览器演示仍需单独验收

按 README 启动三后端与前端，在产品页输入人数、预算、时段、偏好并发送，检查方案 → 确认 → Mock 支付面板的实际状态；恢复场景必须记录具体入选项目与故障配置。准备恢复场景时使用隔离库存副本，不能拿当前原始库存反复扣减。自然语言路径存在模型选择差异，不承诺任意输入必然触发缺货。

本 PR **没有**提供真实浏览器、模型端到端恢复、生产支付或吞吐压测结果。9/9 是固定单元测试数量，不是业务成功率，测试耗时不是 API 延迟。

## 隐私与提交

原始 `log.txt`、日志备份及 `acceptance-runs/` 默认忽略，提交前检查暂存列表。若测试被修改为包含敏感内容，原始堆栈也可能包含该内容；分享前单独检查报告。`.gitignore` 不会取消已跟踪文件的跟踪。
